# MARKDOWN

# Bento Grid Solution

## Disclaimer

This is the free version of the Frontend Mentor challenge, which does not include a Figma design file. As a result, the layout, spacing, typography, and colors are recreated by visually matching the reference image as closely as possible.

---

## Progress Update

Today's focus was building the overall page structure and layout.

### Completed

- Created the overall page structure using semantic HTML.
- Organized the page into two main layout sections for better structure.
- Built the main page layout using **Flexbox**.
- Created the dashboard using **CSS Grid**.
- Defined the layout with `grid-template-areas`.
- Combined **Flexbox** and **Grid**, using each where it fits best.
- Created a reusable `.box` class for shared card styling.
- Added individual modifier classes for each card.
- Placed all images and content into their corresponding layout areas.
- Started applying the visual styling based on the reference design.
- Added background colors, spacing, borders, and border-radius to match the design.
- Structured the CSS to keep shared styles separate from component-specific styles.
- Completed the desktop layout and matched the original design more closely.
- Styled and positioned all images, including cropped illustrations using overflow and transforms where required.
- Completed the desktop layout.

### Next Steps

- Refactor class names to be more descriptive.
- Improve semantic HTML where appropriate (`article`, heading hierarchy, etc.).
- Implement responsive design for tablet and mobile devices.
- Perform a final code refactor and optimization.
- Expand CSS variables (spacing, typography, border-radius, etc.).
- Make the page responsive.
- Final code cleanup and optimization.

 
 # MARKDOWN
> This project is part of my Frontend Mentor practice and is intended to improve my HTML, CSS, Flexbox, and CSS Grid skills.

